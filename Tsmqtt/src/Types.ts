export type connectType = {
    host: string
    port: number,
    protocol: string,
    username: string,
    password: string,
};

export type mysqlConnectType = {
    host: string,
    user: string,
    password: string,
    database: string
}